package fundamentos;

public class DesafioOperadores {
	public static void main(String[] args) {
		int expA = (6 * (int) (Math.pow(3+2, 3)) ) / (3*2); 
		int expB = ((1-5)*(2-7))/2;
		int expC = (int) Math.pow((int)Math.pow(expB, 2), 3); 
	}
}
